import React from 'react';
import LibrariesContainer from '../components/Libraries';

const Libraries = (screenProps) => {
  console.log(screenProps)
  return <LibrariesContainer {...screenProps} />
}

export default Libraries;
